base {
 log_debug = off;
 log_info = on;
 log = stderr;
 daemon = on;
 user = nogroup;
 group = nogroup;
 redirector = iptables;
}
redsocks {
 local_ip = 127.0.0.1;
 local_port = <%= p('port') %>;
 ip = <%= p('remote-proxy.ip') %>;
 port = <%= p('remote-proxy.port') %>;
 type = http-connect;
}
