base {
 log_debug = on;
 log_info = on;
 log = stderr;
 redirector = iptables;
}
redsocks {
 local_ip = <%= p('local-ip') %>;
 local_port = <%= p('port') %>;
 ip = <%= p('remote-proxy.ip') %>;
 port = <%= p('remote-proxy.port') %>;
 type = http-connect;
}
