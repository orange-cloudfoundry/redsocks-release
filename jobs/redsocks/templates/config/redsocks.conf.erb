base {
 log_debug = off;
 log_info = on;
 daemon = on;
 user = vcap;
 group = vcap;
 redirector = iptables;
}
redsocks {
 local_ip = 127.0.0.1;
 local_port = <%= p('port') %>;
 ip = <%= p('remote-proxy.ip') %>;
 port = <%= p('remote-proxy.ip') %>;
 type = http-connect;
}
