#!/bin/bash

set -u # report the usage of uninitialized variables

sudo iptables -t nat -D PREROUTING -i eth0 -p tcp --dport 443 -j REDSOCKS
sudo iptables -X REDSOCKS

EXITSTATUS=0

echo " post-stop is complete; exit status $EXITSTATUS"
exit $EXITSTATUS
